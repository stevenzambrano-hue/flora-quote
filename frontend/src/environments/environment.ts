// Production environment — used by default in `ng build`
export const environment = {
  production: true,
  apiUrl: 'https://flora-quote.onrender.com/api',
  apiV1Url: 'https://flora-quote.onrender.com/api/v1',
};
