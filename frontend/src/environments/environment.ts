// Production environment — used by default in `ng build`
export const environment = {
  production: true,
  apiUrl: 'https://YOUR_BACKEND_URL/api',       // ← reemplazar con la URL de Render/Railway
  apiV1Url: 'https://YOUR_BACKEND_URL/api/v1',  // ← reemplazar con la URL de Render/Railway
};
